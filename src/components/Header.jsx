 import './Main.css'

export const Header = ({name}) => {
    return (
        <div>
            <h1>{name}</h1>
            <h2 className="samuel">Presentacion de mi portafolio</h2>
        </div>
    )
  }
  
